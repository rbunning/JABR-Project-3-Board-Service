spring:
  application:
    name: Zuul-Service
  cloud:
    config:
      uri:  http://localhost:8888
      
zuul:
  routes:
    userservice:
      path: /user/**
      sensitiveHeaders:
      serviceId: user-service
