spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
  mvc:
    dispatchOptionsRequest: true 
#server.contextPath: /auth
keystore:
 password: mySecretKey
