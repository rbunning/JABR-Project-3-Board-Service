server:
  port: 8888
  
spring:
  application:
    name: Config-Service
  datasource:
    tomcat: 
      initial-size: 1
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rbunning/JABR-Project-3-config-yml-files.git
      
      
      
      
